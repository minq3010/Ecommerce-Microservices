apiVersion: v1
kind: Service
metadata:
  name: cart-service
spec:
  ports:
  - port: 8005
    targetPort: 8005
    name: http
  - port: 50052
    targetPort: 50052
    name: grpc
  selector:
    app: cart-service
  type: ClusterIP
